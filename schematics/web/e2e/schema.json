{
  "$id": "E2ESchematics",
  "title": "E2E module",
  "type": "object",
  "description": "Creates E2E tests",
  "properties": {
    "e2eModule": {
      "description": "Modules",
      "uniqueItems": true,
      "default": ["Cypress"],
      "x-prompt": {
        "message": "Which tool do you want to use to automate e2e tests? You can add this later.",
        "type": "list",
        "items": ["Cypress"]
      }
    }
  },
  "required": ["e2eModule"]
}
